{
  "name": "@basement-works/link.bsmnt.pro",
  "version": "1.0.0",
  "description": "a serverless & minimalist URL shortener that doesn't try to do too much.",
  "main": "index.js",
  "scripts": {
    "dev": "npx eleventy --serve",
    "build": "npx eleventy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "devDependencies": {
    "@11ty/eleventy": "1.0.2"
  }
}
